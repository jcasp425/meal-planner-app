var React = require('react');
var MacroInput = require('./macroInput');

module.exports = React.createClass({
  render: function() {
    return <div className="list-group">
    	<button type="button" className="list-group-item">Placeholder</button>
    </div>
  }
});
